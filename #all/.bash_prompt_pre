# GPG settings
if [ -f "${HOME}/.gpg-agent-info" ]
then
  source "${HOME}/.gpg-agent-info"
  #export GPG_AGENT_INiFO
fi

# SSH settings
[ -f "${HOME}/.ssh-agent-info" ] && source "${HOME}/.ssh-agent-info" > /dev/null

